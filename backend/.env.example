# ===========================================
# SparkFlow Backend Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values

# Application Settings
# --------------------
DEBUG=true
HOST=0.0.0.0
PORT=8000

# Security
# --------
# IMPORTANT: Change this to a secure random string in production!
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database
# --------
DATABASE_URL=sqlite:///./data.db

# LLM Configuration (Aliyun Tongyi Qianwen)
# -----------------------------------------
# Get your API key from: https://dashscope.console.aliyun.com/
LLM_PROVIDER=qwen
LLM_MODEL=qwen-turbo
DASHSCOPE_API_KEY=sk-your-dashscope-api-key-here

# STT Configuration (DashScope / 阿里云百炼 - 推荐)
# --------------------------------------------------
# 推荐使用阿里云百炼/灵积平台，仅需 DASHSCOPE_API_KEY 即可使用语音识别
# 支持模型: paraformer-v2 (默认), paraformer-realtime-v2
# 注意: 与 LLM 使用相同的 DASHSCOPE_API_KEY，无需额外配置
STT_PROVIDER=dashscope
# 如需单独指定 STT 模型，可取消注释:
# DASHSCOPE_STT_MODEL=paraformer-v2

# STT Configuration (Aliyun NLS - 传统方式)
# -----------------------------------------
# 如需使用传统 NLS 方式，将 STT_PROVIDER 改为 'aliyun' 并配置以下三项
# Get your credentials from: https://nls-portal.console.aliyun.com/
# ALIBABA_CLOUD_ACCESS_KEY_ID=your-access-key-id
# ALIBABA_CLOUD_ACCESS_KEY_SECRET=your-access-key-secret
# ALIBABA_CLOUD_APP_KEY=your-nls-app-key

# Embedding Configuration
# -----------------------
EMBEDDING_PROVIDER=qwen
EMBEDDING_MODEL=text-embedding-v2

# Vector Database Configuration
# -----------------------------
VECTOR_DB_PROVIDER=chromadb
CHROMADB_PATH=./chroma_data

# Storage Configuration
# ---------------------
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=52428800
